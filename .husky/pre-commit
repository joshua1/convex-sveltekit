# Auto-bump patch version on main branch
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  npm version patch --no-git-tag-version > /dev/null 2>&1
  git add package.json package-lock.json
fi
